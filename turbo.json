{
  "$schema": "https://turbo.build/schema.json",
  "envMode": "loose",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["**/*.js", "**/*.d.ts", "**/*.map"]
    },
    "test": {
      "dependsOn": ["build"]
    }
  }
}
